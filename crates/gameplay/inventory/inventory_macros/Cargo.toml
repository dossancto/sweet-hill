[package]
name = "inventory_macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
syn = { workspace = true }
quote = { workspace = true }
inventory_core = { version = "0.1.0", path = "../inventory_core" }
bevy = { workspace = true,  default-features = false }
