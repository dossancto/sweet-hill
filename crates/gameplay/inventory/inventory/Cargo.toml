[package]
name = "inventory"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true }
gameplay_input = { version = "0.1.0", path = "../../gameplay_input" }
states = { version = "0.1.0", path = "../../../states" }
